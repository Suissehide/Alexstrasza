{% extends 'base.html.twig' %}

{% block title %}Sondage{% endblock %}

{% block body %}
    <h1>Sondage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sondage.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ sondage.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sondage_index') }}">back to list</a>

    <a href="{{ path('sondage_edit', {'id': sondage.id}) }}">edit</a>

    {{ include('sondage/_delete_form.html.twig') }}
{% endblock %}
